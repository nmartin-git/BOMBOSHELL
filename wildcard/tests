ls *.c - should list all .c files

echo * - should list all non-hidden files, quoting filenames with spaces

echo .* - should list all hidden files

echo '*.c' - should print "*.c" literally

echo "*" - should print "*" literally

echo no_wildcard - should print "no_wildcard" normally

echo "file with spaces.c" - should handle filenames with spaces correctly

utilise cette fct pour l'exec char	**expand_args_wildcards(char **args);

